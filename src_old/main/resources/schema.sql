CREATE TABLE cbs.cbsuser
  (  
     cbs_user_id	    int,
     name               varchar(255),
     password           varchar(255),
     email              varchar(255),
     PRIMARY KEY(cbs_user_id)
  );